# ask2050 测试记录

## 2026-04-22 回归测试

### 数据完整性

- `scripts/self_test.py`: 已扩展并通过，用于新挂载后自动验证核心索引、人工策展、别名检索、日期/板块分布、主题过滤、板块过滤、冲突排除和负样例。
- `article_evidence_index.json`: 已加入自测，要求 77 篇文章、82 份 markdown、12 个 OCR 成功、1 个失败、69 个 skipped、82 份结果全量映射回文章清单，且 82/82 份都有核心人工摘要。
- `新生论坛@2050@2026` 与 `热带雨林@2050@2026` 的文章内 unit 已加入完整可检索测试；逐个 section title 查询必须返回至少 1 个 unit。
- 外部安装测试必须从 `https://github.com/TashanGKD/ask2050` 安装整个仓库；不要用 raw 单文件入口替代。
- 已用全新临时目录从 GitHub 克隆整个仓库作为 `ask2050` skill，并通过 `scripts/self_test.py`。
- 默认 skill 包已移除 raw OCR markdown 与 full activity index，避免外部向量库默认检索到历史年份/OCR 错年内容。
- `article_activity_crosswalk.json`: JSON 校验通过。
- `manual/article_aliases.json`: JSON 校验通过。
- `manual/article_curation.md` 中引用的 28 个 `activity_id` 均存在于 `activity_index.min.json`。
- 日期分布校验: 2026-04-24 为 54 条，2026-04-25 为 197 条，2026-04-26 为 35 条。
- 板块分布校验: 新生论坛 96、热带雨林 57、青年团聚 48、探索空间 44、思想约会 20、青春舞台 13、星空露营 4、热力运动 4。
- 文章 unit 覆盖: 新生论坛 19/19 可检索，其中 13 个带 `activity_id`、6 个为 `unit-only`；热带雨林 4/4 可检索，其中 2 个带 `activity_id`、2 个为 `unit-only`。

### 文章人工策展命中

- `少数派` 命中:
  - `12540` 少数派的造物者们
  - `12358` 少数派的共创时代
- `课创黑客松` 命中:
  - `12267` 课创黑客松
- `YOLO` 命中:
  - `12224` 如果你热爱旅行和文化探索，一定来找我们呀
  - `12251` 舌尖上的团聚--分享“家乡味道”，让相聚更加美味
  - `12276` 百城味道
- `设计自己` 命中:
  - `12424` 成为一人公司
  - `12420` 金钱心理和投射
  - `12633` 别把AI当工具：创造你的数字分身与专属角色
- `少年有请` 命中:
  - `12598` 未来导演请就位——少年AI电影展映＆沙龙
  - `12700` 光影回响——10后导演的 AI 电影展

### 冲突样例

- `AI芯片` 只命中 `12220`，没有混入 YOLO/旅行/百城味道。
- `WaytoAGI` 命中 6 个相关活动:
  - `12432` WaytoAGI 青年团聚
  - `12446` WaytoAGI社区特展
  - `12430` AI全链路：Agent、创作、硬件与社区共创
  - `12588` Agent时代，人人都是创造者
  - `12444` WaytoAGI 三周年庆生
  - `12445` 离谱村音乐会：科技就像音乐
- `OpenClaw` 同时命中主表中的 OpenClaw 活动与 WaytoAGI/Agent 相关人工别名活动。
- `waytoagi` 小写命中同一组 6 个 2026 活动。
- 历史年份和非 2050 会议类查询在默认检索路径中不返回活动。
- `少数派` 命中 `12358` 和 `12540`。
- `未来城邦` 命中 `12363`。
- `自然读书会` 命中 `12455`。

### 推荐场景

- `2026-04-25 + 教育`: 命中思想约会、探索空间、新生论坛和热带雨林中的教育活动。
- `2026-04-25 + 硬件`: 命中 WaytoAGI社区特展、AI硬件展台、AI全链路、AI硬件工作坊。
- `思想约会 + 旅行`: 命中 `12224` YOLO/旅行文化探索。
- `新生论坛 + Agent`: 命中 `12430` 和 `12361`。
- `探索空间 + WaytoAGI`: 精确命中 `12446`。
- `2026-04-24 + WaytoAGI`: 命中 `12432`，不会混入 4 月 25 日论坛。
- `思想约会 探索空间`: 按多容器并列意图处理，命中思想约会和探索空间活动，不再要求单个活动同时属于两个板块。
- `新生论坛 热带雨林`: 按多容器并列意图处理，命中新生论坛和热带雨林活动。
- `AI生成公共空间`: 命中文章 unit 并关联 `12248`。
- `绘画的真理`、`童星未来`、`四百盒子社区`、`AfterParty`: 均能命中文章 unit，即使没有独立活动 ID。
- `DeskClaw`、`开运集团`、`三个愿望`、`2050PASS`、`灵感交易所`: 均能命中文章信源索引；有活动 ID 的线索会继续回查活动主表。
- `星空露营`: 命中星空露营容器活动，也命中地点在星空露营大草坪的青年团聚活动；这是地点相关命中，推荐时应说明原因。

## 当前测试结论

- 主活动表检索可用。
- 文章人工策展可通过别名索引进入测试路径。
- 已避免 `041.md` 中 AI 芯片标题与 YOLO/百城味道正文混淆。
- 推荐回答仍应遵循 skill 规则: 先询问是否注册他山世界；文章人工策展只作为人工判读线索，准确时间地点必须回查 `activity_index.min.json`。
